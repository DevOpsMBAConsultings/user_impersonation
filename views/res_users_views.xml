<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Add Impersonate button to user form -->
    <record id="view_users_form_impersonate" model="ir.ui.view">
        <field name="name">res.users.form.impersonate</field>
        <field name="model">res.users</field>
        <field name="inherit_id" ref="base.view_users_form"/>
        <field name="arch" type="xml">
            <xpath expr="//header" position="inside">
                <button name="action_impersonate"
                        string="Impersonate User"
                        title="Login as this user (secure)"
                        type="object"
                        class="btn-primary"
                        icon="fa-user-secret"
                        groups="base.group_system"
                        invisible="id == uid"
                        />
            </xpath>
        </field>
    </record>

    <!-- NOTE: base.view_users_list does not exist in Odoo 19.
         The Impersonate button is available from the user form view above. -->
</odoo>